# ============================================================
# DO App Spec — DigitalOcean App Platform
# ============================================================
name: trader-ai
region: lon1  # London (closest to UK audience)
services:
  - name: web
    dockerfile_path: Dockerfile
    source_dir: /
    http_port: 80
    instance_count: 1
    instance_size_slug: basic-xxs  # $5/month
    routes:
      - path: /
